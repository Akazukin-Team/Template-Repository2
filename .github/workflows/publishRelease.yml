name: Publish release to Repository

on:
  release:
    types: [ published ]


env:
  JDK_VERSION: 25

jobs:
  build:
    runs-on: ubuntu-24.04

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Cache
        uses: actions/cache@v5
        with:
          path: ~/.gradle
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle') }}
          restore-keys: ${{ runner.os }}-gradle

      - name: Setup JDK ${{ env.JDK_VERSION }}
        uses: actions/setup-java@v5
        with:
          java-version: ${{ env.JDK_VERSION }}
          distribution: 'adopt'

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Clean cache
        run: ./gradlew clean

      - name: Build and Test
        run: ./gradlew build check

      - name: Publish to Nexus
        run: ./gradlew publishReleasePublicationToReleasesRepository publishSnapshotPublicationToSnapshotsRepository -Dorg.gradle.unsafe.configuration-cache=false
        env:
          REPOSITORY_USERNAME: ${{ secrets.NEXUS_COMMON_PLAIN_USERNAME }}
          REPOSITORY_PASSWORD: ${{ secrets.NEXUS_COMMON_PLAIN_PASSWORD }}
